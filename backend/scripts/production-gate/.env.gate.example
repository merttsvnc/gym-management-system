# =============================================================================
# Production Gate - Environment (DEV database only)
# =============================================================================
# 1. cp .env.gate.example .env.gate
# 2. Fill all required values below
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED
# -----------------------------------------------------------------------------
# DEV database - used for production rehearsal (schema identical to production)
# NEVER use gym_management_test
DATABASE_URL_DEV=postgresql://USER:PASSWORD@HOST:5432/gym_management_dev?schema=public

# App boot (validated by 01_env_check)
NODE_ENV=development
PORT=3000
JWT_ACCESS_SECRET=your_jwt_access_secret_min_32_chars_required

# -----------------------------------------------------------------------------
# OPTIONAL (for smoke tests, CORS, etc.)
# -----------------------------------------------------------------------------
# CORS_ORIGINS=http://localhost:5173
# FRONTEND_URL=http://localhost:5173
# APP_VERSION=1.0.0
